<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
   {# this is what's new #}
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>{% if title %}{{ title }} | {% endif %}My Portfolio</title>
  <link rel="stylesheet" href="/styles.css" />
</head>
<body>
  {% include "nav.njk" %}
  <main>
    {{ content | safe }}
  </main>
</body>
</html>
